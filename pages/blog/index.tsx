//file name should be same as the url path is same as the file name
//like here the file name is blog so url path would be '/blog'
const blog = () => {
  return (
    <div>blog1</div>
  )
}

export default blog
